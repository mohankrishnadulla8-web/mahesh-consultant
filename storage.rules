rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /student_documents/{allPaths=**} {
      allow write: if request.resource.size < 5 * 1024 * 1024
                   && (request.resource.contentType.matches('image/.*') 
                       || request.resource.contentType == 'application/pdf');
      allow read: if false;
    }
  }
}
